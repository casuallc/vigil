## 🧪 RabbitMQ 队列绑定集成测试用例

> **测试前提**：
> - 使用 AMQP 0-9-1 协议
> - RabbitMQ 服务器正常运行
> - 每个测试使用独立连接和通道
> - 测试完成后自动清理创建的资源

---

### ✅ 分类一：基本绑定操作

| ID | 描述 | Exchange | Queue | Binding Key | Expected Result |
|----|------|----------|-------|-------------|----------------|
| RB-BIND-01 | 成功绑定队列到交换器 | `test-exchange` | `test-queue` | `bind-key` | ✅ 绑定成功 |
| RB-BIND-02 | 成功解绑队列 | `test-exchange` | `test-queue` | `bind-key` | ✅ 解绑成功 |
| RB-BIND-03 | 多次绑定同一队列 | `test-exchange` | `test-queue` | `bind-key` | ✅ 多次绑定不影响功能 |
| RB-BIND-04 | 解绑不存在的绑定 | `test-exchange` | `test-queue` | `non-existent-key` | ✅ 无错误（幂等操作） |

---

### ✅ 分类二：绑定与路由验证

| ID | 描述 | Exchange | Queue | Binding Key | Publish Routing Key | Expected Result |
|----|------|----------|-------|-------------|-------------------|----------------|
| RB-BIND-05 | 绑定后能收到消息 | `test-exchange` | `test-queue` | `bind-key` | `bind-key` | ✅ 消息路由到队列 |
| RB-BIND-06 | 解绑后不能收到消息 | `test-exchange` | `test-queue` | `bind-key` | `bind-key` | ❌ 消息不路由到队列 |
| RB-BIND-07 | 绑定不同路由键 | `test-exchange` | `test-queue` | `key1`, `key2` | `key1`, `key2` | ✅ 两条消息都路由到队列 |
| RB-BIND-08 | 绑定通配符路由键 | `test-topic` | `test-queue` | `sensor.#` | `sensor.temp.room1` | ✅ 消息路由到队列 |

---

### ✅ 分类三：绑定属性与参数

| ID | 描述 | Exchange | Queue | Binding Args | Expected Result |
|----|------|----------|-------|--------------|----------------|
| RB-BIND-09 | 带自定义参数的绑定 | `test-exchange` | `test-queue` | `{"arg1":"value1"}` | ✅ 绑定成功 |
| RB-BIND-10 | 绑定到默认交换器 | `(default)` | `test-queue` | N/A | ✅ 消息直接路由到同名队列 |
| RB-BIND-11 | 绑定到不存在的交换器 | `non-existent-exchange` | `test-queue` | `bind-key` | ❌ 绑定失败 |

---

### ✅ 分类四：多队列绑定场景

| ID | 描述 | Exchange | Queues | Binding Keys | Publish Routing Key | Expected Result |
|----|------|----------|--------|--------------|-------------------|----------------|
| RB-BIND-12 | 多队列绑定到同一交换器 | `test-exchange` | `queue1`, `queue2` | `key1` | `key1` | ✅ 两条消息分别路由到两个队列 |
| RB-BIND-13 | 队列绑定到多个交换器 | `exchange1`, `exchange2` | `test-queue` | `key1` | `key1` | ✅ 从两个交换器发布的消息都能收到 |

---

## ✅ 总结：覆盖维度

| 维度 | 是否覆盖 |
|------|--------|
| 基本绑定与解绑 | ✅ |
| 绑定后的消息路由 | ✅ |
| 解绑后的消息路由 | ✅ |
| 多次绑定处理 | ✅ |
| 通配符绑定 | ✅ |
| 带参数的绑定 | ✅ |
| 默认交换器绑定 | ✅ |
| 多队列绑定 | ✅ |
| 队列绑定多个交换器 | ✅ |

---