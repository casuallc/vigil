# 配置管理命令

配置管理命令用于查看和管理系统配置。

## 命令格式

```
bbx-cli config [command]
```

## 命令列表

### get - 获取配置

获取当前系统配置。

**用法：**
```
bbx-cli config get
```

**示例：**
```bash
# 查看当前配置
./bbx-cli config get -H http://localhost:8181
```
